# Dependencies — reinstalled on the EAS server / by yarn
node_modules/
.npmrc

# Repository-level instruction file (local-only, not tracked)
/AGENTS.md

# Expo local state (not source of truth, regenerated)
.expo/
.expo-shared/
dist/
web-build/
expo-env.d.ts
app-example

# Metro
.metro-cache/
.metro-health-check*

# Test coverage artifacts
**/coverage/
*.test-results.xml

# Native build outputs (source under android/, ios/ is tracked)
**/build/
**/.gradle/
**/.cxx/
android/build/
android/app/build/
android/.gradle/
android/.kotlin/
android/app/.cxx/
android/local.properties
**/local.properties
ios/build/
ios/Pods/

# Build output / dist artifacts
*.apk
*.aab
*.jsbundle

# Caches and temporary files
.cache/
.turbo/
.tmp/
tmp/
temp/
.temp/
%TEMP%/
*.log
npm-debug.*
yarn-debug.*
yarn-error.*
*.tsbuildinfo

# Media and archive downloads — not in source control
*.mp4
*.mov
*.avi
*.zip
*.tar
*.gz

# Secrets MUST stay out of the repository
**/keystore.properties
*.jks
*.keystore
*.p8
*.p12
*.key
*.pem
*.mobileprovision
*.orig.*

# Local env files
.env*.local

# Development-only docs/reports (kept locally, not tracked)
frontend/docs/

# Local tooling state
.kilo/
.claude/
.opencode/

# Editor / OS junk
.vscode/
.idea/
*.iml
*.hprof
.DS_Store
Thumbs.db